pipeline {
    agent any

    stages {
        stage('Nettoyage du Workspace') {
            steps {
                
                cleanWs()
            }
        }
        
        stage('Checkout SCM') {
            steps {
                
               
                git branch: 'main', url: 'https://github.com/ThomasDufour78/githubeval.git'
            }
        }
    }
}
